# File generated by protoc-gen-pocket-openapi. DO NOT EDIT.
openapi: 3.0.0
info:
  title: store-resource
  version: "0.1.0"



paths:
  /store-resource/v1/orders:
    post:
      summary: Cria um novo pedido.
      description: Cria um novo pedido.
      operationId: CreateOrder
      requestBody:
        required: true
        description: 
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOrderRequest"
      responses:
        '200':
          description: Sucesso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateOrderResponse"
        '400':
          description: Argumentos da requisição inválidos.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"
        '401':
          description: Sem autorização para acesso ao recurso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DefaultError"
        '500':
          description: Erro interno do servidor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DefaultError"
  /store-resource/v1/orders/{id}:
    get:
      summary: Obtém informações de um determinado pedido.
      description: Obtém detalhes e informações de um determinado pedido.
      operationId: GetOrder
      parameters:
        - in: path
          name: "id"
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Sucesso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetOrderResponse"
        '400':
          description: Argumentos da requisição inválidos.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"
        '401':
          description: Sem autorização para acesso ao recurso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DefaultError"
        '500':
          description: Erro interno do servidor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DefaultError"
  /store-resource/v1/pets:
    post:
      summary: Cria um novo Pet.
      description: Cria um novo Pet.
      operationId: CreatePet
      requestBody:
        required: true
        description: 
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePetRequest"
      responses:
        '200':
          description: Sucesso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreatePetResponse"
        '400':
          description: Argumentos da requisição inválidos.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"
        '401':
          description: Sem autorização para acesso ao recurso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DefaultError"
        '500':
          description: Erro interno do servidor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DefaultError"
  /store-resource/v1/pets-list:
    get:
      summary: Obtém informações de todos Pets cadastrados.
      description: Obtém detalhes e informações de todos Pet cadastrados.
      operationId: GetPets
      responses:
        '200':
          description: Sucesso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetPetsResponse"
        '400':
          description: Argumentos da requisição inválidos.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"
        '401':
          description: Sem autorização para acesso ao recurso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DefaultError"
        '500':
          description: Erro interno do servidor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DefaultError"
  /store-resource/v1/pets/{id}:
    delete:
      summary: Apaga um Pet.
      description: Apaga um Pet.
      operationId: DeletePet
      parameters:
        - in: path
          name: "id"
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Sucesso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeletePetResponse"
        '400':
          description: Argumentos da requisição inválidos.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"
        '401':
          description: Sem autorização para acesso ao recurso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DefaultError"
        '500':
          description: Erro interno do servidor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DefaultError"
    get:
      summary: Obtém informações de um determinado Pet.
      description: Obtém detalhes e informações de um determinado Pet.
      operationId: GetPetByID
      responses:
        '200':
          description: Sucesso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetPetByIDResponse"
        '400':
          description: Argumentos da requisição inválidos.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"
        '401':
          description: Sem autorização para acesso ao recurso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DefaultError"
        '500':
          description: Erro interno do servidor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DefaultError"
    patch:
      summary: Atualiza um Pet.
      description: Atualiza um Pet.
      operationId: UpdatePet
      parameters:
        - in: path
          name: "id"
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Sucesso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdatePetResponse"
        '400':
          description: Argumentos da requisição inválidos.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"
        '401':
          description: Sem autorização para acesso ao recurso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DefaultError"
        '500':
          description: Erro interno do servidor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DefaultError"

components:
  schemas:
    CreateOrderRequest:
      type: object
      properties:
        pets_ids:
          type: array
          items:
            type: string
    CreateOrderResponse:
      type: object
      properties:
        order:
          $ref: '#/components/schemas/OrderProto'
    CreatePetRequest:
      type: object
      properties:
        name:
          type: string
    CreatePetResponse:
      type: object
      properties:
        pet:
          $ref: '#/components/schemas/PetProto'
    DefaultError:
      type: object
      properties:
        errors:
          type: array
          items:
            type: string
        message:
          type: string
    DeletePetResponse:
      type: object
      properties:
        pet:
          $ref: '#/components/schemas/PetProto'
    FieldValidationError:
      type: object
      properties:
        field:
          type: string
        location:
          type: string
        message:
          type: string
    GetOrderResponse:
      type: object
      properties:
        order:
          $ref: '#/components/schemas/OrderProto'
    GetPetByIDResponse:
      type: object
      properties:
        pet:
          $ref: '#/components/schemas/PetProto'
    GetPetsResponse:
      type: object
      properties:
        pagination:
          $ref: '#/components/schemas/PaginationResponseProto'
        pets:
          type: array
          items:
            $ref: '#/components/schemas/PetProto'
    OrderProto:
      type: object
      properties:
        created_at:
          type: string
          format: date-time
        id:
          type: string
        pets_ids:
          type: array
          items:
            type: string
    PaginationResponseProto:
      type: object
      properties:
        page:
          type: integer
        page_size:
          type: integer
    PetProto:
      type: object
      properties:
        created_at:
          type: string
          format: date-time
        deleted_at:
          type: string
          format: date-time
        id:
          type: string
        name:
          type: string
        updated_at:
          type: string
          format: date-time
    UpdatePetResponse:
      type: object
      properties:
        pet:
          $ref: '#/components/schemas/PetProto'
    ValidationError:
      type: object
      properties:
        errors:
          type: array
          items:
            $ref: '#/components/schemas/FieldValidationError'
        message:
          type: string 
  securitySchemes:
    authorization:
      type: http
      scheme: unspecified

